'use client';

import { Collapsible as CollapsiblePrimitive } from '@base-ui/react/collapsible';

/** Base UI Collapsible parts. Style the panel with `data-postext-collapsible`
 *  (height animation from `SANDBOX_CSS`); pass `data-instant` to skip it. */
export const Collapsible = CollapsiblePrimitive;
