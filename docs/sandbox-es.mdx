{/* docs/sandbox-es.mdx */}

export const metadata = {
  title: 'Sandbox',
  sidebarTitle: 'Sandbox',
  description: 'Entorno interactivo para experimentar con el motor de maquetación de Postext',
  lang: 'es',
  lastUpdated: '2026-04-23',
  readingTime: '6 min',
  order: 7,
};

# Sandbox

**Un entorno interactivo para el motor de maquetación de Postext.**

El Sandbox ofrece una interfaz estilo VS Code donde puedes escribir markdown, ajustar la configuración de maquetación y ver los resultados renderizados en tiempo real a través de tres modos de salida sincronizados — **canvas**, **HTML** y **PDF**. Está pensado para la experimentación: afina los ajustes de tipografía, cambia el número de columnas, prueba estrategias de colocación de recursos y observa cómo responde el motor.

## Disposición de la interfaz

El Sandbox sigue un paradigma IDE familiar con tres áreas principales: una **barra de actividad** estrecha para cambiar de panel, un **panel lateral** redimensionable con el editor o la configuración, y un **viewport** a la derecha con las tres pestañas de salida.

<Illustration name="SandboxLayout" data='{"title": "Interfaz del Sandbox", "desc": "Barra de actividad de 48 px a la izquierda, panel lateral redimensionable en el centro y viewport a la derecha con pestañas Canvas, HTML y PDF.", "caption": "Tres áreas: barra de actividad, panel lateral redimensionable y viewport con pestañas.", "activityBar": "Actividad", "activityIcons": ["MD", "⚙", "📁", "⇣", "⇡"], "sidebarTitle": "Panel lateral", "sidebarSubtitle": "editor o configuración", "viewportTitle": "Viewport", "tabs": ["Canvas", "HTML", "PDF"], "resizeHint": "arrastra para redimensionar"}' />

### Barra de actividad

Una tira estrecha en el borde izquierdo (48 px de ancho) contiene botones con iconos para navegar entre paneles. La sección superior aloja tres interruptores de panel:

- **Editor de markdown** (icono `FileCode`) — Abre el panel de edición de markdown
- **Configuración** (icono `Settings2`) — Abre el panel de configuración de la maquetación
- **Recursos** (icono `FolderOpen`) — Abre el panel de gestión de recursos (próximamente)

Pulsar el icono del panel activo colapsa la barra lateral. Pulsar un icono distinto cambia a ese panel.

La sección inferior de la barra de actividad contiene:

- **Exportar** — Descarga el estado actual del sandbox (markdown + configuración) como archivo JSON
- **Importar** — Carga un archivo JSON exportado previamente
- **Conmutador de tema** — Alterna entre modo oscuro y claro
- **Selector de idioma** — Cambia el idioma de la interfaz

### Paneles laterales

La barra lateral es redimensionable: arrastra el separador entre la barra lateral y el viewport para ajustar su anchura (rango 240 px–480 px). La barra lateral se colapsa por completo cuando no hay ningún panel activo.

#### Panel de configuración

Un editor basado en formulario para `PostextConfig`. Los ajustes se agrupan en secciones colapsables, cada una con un botón de restablecer para recuperar los valores por defecto:

| Sección | Ajustes |
|---------|---------|
| **Página** | Color de fondo, tamaño predefinido (o ancho/alto personalizados), márgenes (superior, inferior, izquierdo, derecho), DPI, líneas de corte, rejilla de línea base (con color y grosor de línea) y una subsección **Numeración** (formato + valor inicial) para las etiquetas de página |
| **Maquetación** | Tipo de maquetación (una columna, doble, columna y media), ancho del medianil, porcentaje de columna lateral |
| **Texto de cuerpo** | Familia tipográfica (con búsqueda de Google Fonts), tamaño de fuente, interlineado, color de texto, alineación, peso de fuente, peso de la negrita, separación silábica (interruptor + locale) |
| **Encabezados** | Valores por defecto generales (familia tipográfica, interlineado, color, alineación, peso, márgenes) más sobrecargas por nivel (H1–H6) para tamaño de fuente, interlineado, familia tipográfica, peso, color, márgenes, un interruptor **Romper antes** con selector de paridad opcional, un selector **Span** (`column` o `page`), y un interruptor **Diseño avanzado** que abre un editor de ranura de diseño por nivel (elementos de texto, línea y caja con posicionamiento por anclaje, modos de desbordamiento, separación silábica y marcadores `{titleText}` / `{number}` / `{numberRoman}`) |
| **Encabezado y pie** | Listas por bloque de elementos de texto, línea y caja. Cada elemento de texto expone su contenido (con marcadores `{title}`, `{chapterTitle}`, `{pageNumber}`, `{totalPages}`), controles de fuente y color, paridad (todas las páginas / impares / pares), un selector de **Desbordamiento** (envolver, elipsis al principio/centro/final, recortar), un interruptor de **Separación silábica** y un editor de posicionamiento por anclaje + desplazamiento + tamaño para ajuste fino |

Los controles son contextuales: el ancho del medianil solo aparece en maquetaciones multicolumna, los ajustes de separación silábica solo aparecen cuando el texto está justificado, y el porcentaje de columna lateral solo se muestra en la maquetación de columna y media. Los cambios surten efecto inmediatamente en la vista previa del viewport. Para una referencia completa de todas las opciones de configuración y sus valores por defecto, consulta la página de [Configuración](/es/docs/configuration).

#### Editor de markdown

Un editor basado en CodeMirror 6 con:

- **Resaltado de sintaxis** — Encabezados, énfasis, enlaces, bloques de código y citas quedan visualmente diferenciados
- **Números de línea** y resaltado de la línea activa
- **Ajuste de línea** para párrafos largos
- **Barra de herramientas** con botones para acciones markdown habituales:
  - Negrita, cursiva
  - Niveles de encabezado 1–3
  - Enlace, código en línea
  - Cita
  - Listas ordenadas y no ordenadas

La barra de herramientas es extensible: pueden añadirse acciones personalizadas para micro-formatos específicos de postext a medida que evoluciona la especificación de markdown enriquecido.

#### Panel de recursos

*Próximamente en una versión futura.* Este panel gestionará los recursos (imágenes, tablas, figuras, citas destacadas) referenciados desde el contenido markdown.

### Viewport principal

El lado derecho de la interfaz muestra la salida renderizada. Hay tres pestañas disponibles, una por cada modo de renderizado — **Canvas**, **HTML** y **PDF**. Cambiar de pestaña no vuelve a ejecutar la composición: el mismo VDT alimenta a los tres backends, de modo que el contenido que ves es consistente entre los tres modos.

Cada viewport ejecuta el pipeline de composición dentro de un **Web Worker** dedicado (`postext/worker`). Un hook `useLayoutWorker` por viewport posee el handle del worker, envía las fuentes como `ArrayBuffer`s transferibles y dirige los builds con **cancelación last-wins** — una pulsación de tecla o un arrastre de slider aborta el `AbortSignal` del build anterior antes de que empiece el siguiente, por lo que teclear rápido nunca acumula trabajo obsoleto en el hilo principal. Desmontar un viewport libera su worker y rechaza cualquier build pendiente. Consulta [Configuración → Ejecutar la composición en un Web Worker](/es/docs/configuration#ejecutar-la-composición-en-un-web-worker) para el patrón de integración que se usa aquí.

<Illustration name="SandboxSyncFlow" data='{"title": "Un único VDT, tres salidas", "desc": "El markdown y la configuración del sandbox alimentan un único VDT en memoria. Canvas, HTML y PDF se renderizan desde ese mismo VDT, garantizando resultados idénticos entre las tres pestañas.", "caption": "Un mismo VDT alimenta las tres pestañas de salida.", "markdownLabel": "Markdown", "configLabel": "Configuración", "vdtTitle": "VDT", "vdtSubtitle": "composición compartida", "canvasLabel": "Canvas", "htmlLabel": "HTML", "pdfLabel": "PDF", "singleSource": "fuente única de verdad"}' />

#### Pestaña Canvas

Renderiza una vista previa rasterizada del documento en un elemento HTML5 `<canvas>`. Esto ofrece una representación precisa al píxel de cómo se verá el documento compuesto final, incluida la colocación exacta del texto, la maquetación por columnas y la colocación de recursos.

Una barra de herramientas en la parte superior del viewport de canvas proporciona:

- **Controles de zoom** — Aumentar y reducir el zoom (1,25x por paso, rango 0,25x–4x). El zoom manual desactiva el modo de ajuste.
- **Modos de ajuste** — Ajustar al ancho o al alto de la página. Se activan/desactivan; el ajuste se recalcula automáticamente al redimensionar la ventana.
- **Modos de vista** — Página única (una página por fila) o doble página (maquetación estilo libro, donde la primera página aparece a la derecha como recto y las siguientes se emparejan como dobles páginas).

Las páginas se renderizan de forma perezosa mediante `IntersectionObserver` — solo las páginas visibles (más un margen de 200 px) se dibujan en canvas, manteniendo el uso de memoria de GPU bajo para documentos largos.

#### Pestaña HTML

Renderiza la salida de postext dentro de un contenedor Shadow DOM, garantizando un aislamiento completo de estilos respecto a la página anfitriona. El CSS editorial (tipografía, espaciado, filetes de columna) se inyecta dentro del shadow root para que no pueda verse afectado por los estilos de la aplicación circundante ni filtrarse hacia ellos.

Una barra de herramientas en la parte superior del viewport ofrece:

- **Controles de escala de fuente** — Reduce o aumenta el texto del cuerpo conservando cada medida relativa de la configuración.
- **Modos de columna** — Columna única (una sola columna alta y desplazable verticalmente) o multi-columna (tantas columnas como el viewport encaje a la medida configurada, con scroll-snap horizontal entre ellas).

La geometría de las columnas está controlada por la sección **Visor HTML** del panel de configuración: `maxCharsPerLine` define la medida objetivo en caracteres de la fuente del cuerpo, `columnGap` el espacio horizontal entre columnas en modo multi y `optimalLineBreaking` alterna el algoritmo Knuth–Plass dentro del visor. Consulta la [referencia de configuración del Visor HTML](/es/docs/configuration#visor-html) y el [ejemplo de integración completo](/es/docs/configuration#integrar-el-visor-html) — el mismo patrón que usa internamente el sandbox.

#### Pestaña PDF

Genera un PDF real del documento actual mediante el paquete `postext-pdf` y lo muestra dentro del visor PDF integrado del navegador. El PDF se reconstruye automáticamente al montar la pestaña por primera vez y en cada regeneración posterior, usando el mismo VDT que alimenta a las pestañas de canvas y HTML — de modo que los saltos de línea, las fronteras entre páginas y la colocación de recursos coinciden al píxel entre los tres backends.

Una barra de herramientas flotante en la esquina inferior derecha del viewport proporciona:

- **Regenerar** (icono `RefreshCw`) — Reconstruye el PDF a partir del markdown y la configuración actuales. Útil tras cambiar un ajuste que no dispara un rebuild automáticamente, o cuando las fuentes acaban de terminar de cargarse.
- **Descargar** (icono `Download`) — Guarda el PDF actual en disco. El nombre del archivo se deriva del primer encabezado H1 del documento (con `document.pdf` como valor por defecto).
- **Imprimir** (icono `Printer`) — Abre el diálogo de impresión del navegador sobre el visor PDF integrado, que en la mayoría de navegadores permite imprimir o volver a guardar como PDF.

Internamente el sandbox usa un helper `createPdfFontProvider()` que descarga los archivos WOFF2 estáticos por peso desde la CDN de Fontsource en jsdelivr, los descomprime a TTF en el navegador y entrega los bytes a `pdf-lib` a través de `renderToPdf`. Se usan fuentes estáticas por peso (en lugar de una única fuente variable) para que el texto en negrita se incruste realmente con el peso bold. Consulta [Generación de PDF](/es/docs/configuration#generación-de-pdf) para el mismo patrón fuera del sandbox.

## Persistencia

El Sandbox guarda automáticamente tu trabajo en el `localStorage` del navegador:

- **Guardado automático** — El contenido markdown y la configuración se guardan tras 1 segundo de inactividad
- **Carga automática** — Al cargar la página, el contenido de la sesión anterior se restaura automáticamente
- **Estado del viewport** — El modo de vista del canvas (única/doble página) y el modo de ajuste (ninguno/ancho/alto) se conservan entre sesiones
- **Estado de secciones** — Se recuerda qué secciones de configuración están expandidas o colapsadas

### Exportar e importar

Para transferir tu trabajo entre dispositivos o compartir configuraciones:

- **Exportar** (icono `Download` en la barra de actividad) — Descarga un archivo `postext-sandbox.json` que contiene tanto el contenido markdown como la configuración completa
- **Importar** (icono `Upload` en la barra de actividad) — Abre un selector de archivos para cargar un `.json` exportado previamente

El formato de exportación está versionado por compatibilidad hacia adelante:

```json
{
  "version": 1,
  "config": { ... },
  "markdown": "# Tu contenido..."
}
```

## Uso standalone

El Sandbox está disponible como un paquete React independiente (`postext-sandbox`) que puede embeberse en cualquier aplicación React:

```bash
npm install postext-sandbox postext
```

```tsx
import { PostextSandbox } from 'postext-sandbox';

function App() {
  return (
    <PostextSandbox
      initialMarkdown="# Hola Mundo"
      initialConfig={{ layout: { layoutType: 'double' } }}
      onMarkdownChange={(md) => console.log(md)}
      onConfigChange={(cfg) => console.log(cfg)}
    />
  );
}
```

### Props

| Prop | Tipo | Descripción |
|------|------|-------------|
| `initialMarkdown` | `string` | Contenido markdown inicial |
| `initialConfig` | `PostextConfig` | Configuración de maquetación inicial |
| `className` | `string` | Clase CSS para el contenedor raíz |
| `labels` | `Partial<SandboxLabels>` | Sobrecargas de etiquetas i18n (por defecto en inglés) |
| `onConfigChange` | `(config) => void` | Callback cuando cambia la configuración |
| `onMarkdownChange` | `(markdown) => void` | Callback cuando cambia el markdown |
| `themeToggle` | `ReactNode` | Componente personalizado para el conmutador de tema |
| `languageSwitcher` | `ReactNode` | Componente personalizado para el selector de idioma |

### Requisitos de CSS

El paquete usa clases utilitarias de Tailwind CSS que hacen referencia a variables CSS personalizadas. La aplicación anfitriona debe proporcionar estas variables:

- `--background`, `--foreground` — Colores base
- `--surface` — Color para superficies elevadas
- `--gilt` — Color de acento (dorado)
- `--slate` — Color para texto atenuado
- `--rule` — Color de bordes/divisores
- `--accent-blue` — Color de enlace/acento
- `--font-mono` — Familia monoespaciada (para el editor de código)

## Roadmap

Las siguientes características están planificadas para versiones futuras:

- **Gestión de recursos** — Subir, gestionar y referenciar imágenes, tablas y figuras desde el editor de markdown
- **Edición colaborativa** — Compartir sesiones del sandbox mediante URL
- **Configuraciones predefinidas** — Plantillas de arranque rápido para escenarios de maquetación habituales (periódico, artículo académico, capítulo de libro)
- **Extensiones personalizadas de la barra de herramientas** — Añadir botones para la sintaxis de markdown enriquecido específica de postext a medida que evolucione la especificación
